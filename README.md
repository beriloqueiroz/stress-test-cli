# Cli Stress Test

## para rodar

  ```bash
    docker compose up -d
  ```

- commando:

  ```bash
    docker run beriloqueiroz/stress-test-cli:latest --url=http://google.com --requests=10 --concurrency=5
  ```
  